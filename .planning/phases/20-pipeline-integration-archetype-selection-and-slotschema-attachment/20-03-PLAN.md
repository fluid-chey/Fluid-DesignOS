---
phase: 20-pipeline-integration-archetype-selection-and-slotschema-attachment
plan: 03
type: execute
wave: 3
depends_on: [20-02]
files_modified:
  - canvas/e2e/archetype-parity.spec.ts
autonomous: false
requirements:
  - PIPE-20-09
  - PIPE-20-10

must_haves:
  truths:
    - "Archetype-based iterations render sidebar fields matching the archetype schema.json fields"
    - "Text editing via sidebar works on archetype-based iterations"
    - "Brush/decoration transform control appears when schema has a non-null brush field"
    - "Export produces valid output from archetype-based iterations"
  artifacts:
    - path: "canvas/e2e/archetype-parity.spec.ts"
      provides: "Playwright E2E tests for editor sidebar parity on archetype-based iterations"
      contains: "archetype"
  key_links:
    - from: "canvas/e2e/archetype-parity.spec.ts"
      to: "canvas/src/server/db-api.ts"
      via: "REST API fixture chain to create test iteration with slot_schema"
      pattern: "api/iterations"
    - from: "canvas/e2e/archetype-parity.spec.ts"
      to: "canvas/src/lib/template-configs.ts resolveSlotSchemaForIteration"
      via: "Watcher serves iteration HTML with resolved schema"
      pattern: "sidebar"
---

<objective>
Verify feature parity between template-based and archetype-based iterations via Playwright E2E tests. Tests create a campaign with an archetype-based iteration (slot_schema populated with archetype schema including fields and brush), then verify the editor sidebar renders correct fields, text editing works, and brush controls appear.

Purpose: Proves the end-to-end chain works — from stored SlotSchema through watcher HTML serving through editor sidebar rendering. This is the final verification that archetype-driven generations produce fully editable creations.
Output: New E2E test file, visual verification checkpoint.
</objective>

<execution_context>
@/Users/cheyrasmussen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cheyrasmussen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-pipeline-integration-archetype-selection-and-slotschema-attachment/20-CONTEXT.md
@.planning/phases/20-pipeline-integration-archetype-selection-and-slotschema-attachment/20-RESEARCH.md
@.planning/phases/20-pipeline-integration-archetype-selection-and-slotschema-attachment/20-01-SUMMARY.md
@.planning/phases/20-pipeline-integration-archetype-selection-and-slotschema-attachment/20-02-SUMMARY.md

<interfaces>
<!-- Key contracts for E2E test fixture setup -->

REST API endpoints for fixture creation (from canvas/src/server/watcher.ts):
```
POST /api/campaigns           -> { name, description? } -> Campaign
POST /api/campaigns/:id/creations -> { creationType, title? } -> Creation
POST /api/creations/:id/slides    -> {} -> Slide
POST /api/slides/:id/iterations   -> { htmlPath, slotSchema?, source, templateId? } -> Iteration
```

From canvas/src/server/db-api.ts (updateIterationSlotSchema -- Plan 01):
```typescript
export function updateIterationSlotSchema(id: string, slotSchema: object): void;
```

From archetypes/stat-hero-single/schema.json (test fixture schema):
```json
{
  "archetypeId": "stat-hero-single",
  "width": 1080,
  "height": 1080,
  "fields": [
    { "type": "text", "sel": ".context-label", "label": "Context Label", "mode": "text", "rows": 1 },
    { "type": "text", "sel": ".stat-number", "label": "Stat Value", "mode": "text", "rows": 1 },
    { "type": "text", "sel": ".headline", "label": "Headline", "mode": "text", "rows": 2 },
    { "type": "text", "sel": ".body-copy", "label": "Body Copy", "mode": "pre", "rows": 4 }
  ],
  "brush": null
}
```

From canvas/e2e/pipeline-e2e.spec.ts (existing E2E pattern for reference):
```typescript
// Phase 19 tests use REST API fixture chain:
// POST /api/campaigns -> POST /api/campaigns/:id/creations -> POST /api/creations/:id/slides -> POST /api/slides/:id/iterations
// Then navigate to the iteration view and verify sidebar/editor behavior
```

Editor sidebar selectors (from canvas/src/store/editor.ts):
- Sidebar container: role="complementary" or a known class for the right sidebar panel
- Slot field labels appear as text within the sidebar
- Brush transform control appears when schema.brush is non-null
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Playwright E2E tests for archetype-based iteration editor parity</name>
  <files>canvas/e2e/archetype-parity.spec.ts</files>
  <read_first>
    - canvas/e2e/pipeline-e2e.spec.ts (existing E2E test patterns -- fixture creation via REST API, page navigation, selector patterns)
    - canvas/playwright.config.ts (base URL, timeout settings)
    - canvas/src/store/editor.ts (sidebar field rendering selectors, how slot fields appear in DOM)
    - canvas/src/server/watcher.ts (lines 1340-1549 -- iteration HTML serving endpoint, how it resolves slot schema and injects transform targets)
    - archetypes/stat-hero-single/index.html (the HTML that would be stored for an archetype-based iteration)
    - archetypes/stat-hero-single/schema.json (the schema to use in test fixture)
  </read_first>
  <action>
Create `canvas/e2e/archetype-parity.spec.ts` with these test cases:

**Test setup (beforeAll):**
1. Create a test campaign via `POST /api/campaigns` with name "E2E Archetype Parity Test"
2. Create a creation via `POST /api/campaigns/:id/creations` with creationType "instagram"
3. Create a slide via `POST /api/creations/:id/slides`
4. Read `archetypes/stat-hero-single/index.html` and write it to the expected iteration HTML path (`.fluid/campaigns/{cId}/{creationId}/{slideId}/{iterationId}.html`)
5. Create an iteration via `POST /api/slides/:id/iterations` with:
   - htmlPath pointing to the written HTML
   - source: "ai"
   - slotSchema: the full stat-hero-single schema.json content with archetypeId, width, height, fields, and brush: null
6. Also create a second iteration with brush: ".brush-element" in the schema to test brush control rendering

**Test: "Sidebar renders archetype schema fields"**
- Navigate to the iteration view (click through campaign > creation > slide > iteration, or use direct URL if available)
- Wait for the sidebar to load
- Verify sidebar contains labels: "Context Label", "Stat Value", "Headline", "Body Copy" (from schema.json fields)
- Each field should have an editable text input or textarea

**Test: "Text editing via sidebar updates iframe content"**
- Navigate to the iteration
- Find the "Headline" sidebar field
- Type a new value: "Updated Headline Text"
- Verify the iframe content updates to show "Updated Headline Text" in the .headline element

**Test: "Brush transform control appears when schema has brush"**
- Navigate to the second iteration (the one with brush: ".brush-element")
- Verify a brush/transform control appears in the sidebar or on the canvas overlay
- Navigate back to the first iteration (brush: null)
- Verify no brush transform control is visible

**Test: "Export works for archetype-based iteration"**
- Navigate to the iteration
- Find the export/download button
- Click it and verify it initiates a download (or verify the /api/iterations/:id/export endpoint responds with 200)

**Cleanup (afterAll):**
- Delete the test campaign (cascade deletes creations/slides/iterations)
- Remove the test HTML file from disk

Follow the patterns in pipeline-e2e.spec.ts for:
- baseURL from playwright config
- request.newContext() for API calls
- page.goto() patterns
- Selector naming conventions used in the existing codebase

Important: The test must work with the Vite dev server running on the configured port. Check playwright.config.ts for `webServer` configuration and `baseURL`.
  </action>
  <verify>
    <automated>cd /Users/cheyrasmussen/Fluid-DesignOS/canvas && npx playwright test e2e/archetype-parity.spec.ts --reporter=list 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - canvas/e2e/archetype-parity.spec.ts exists and contains at least 3 test cases
    - canvas/e2e/archetype-parity.spec.ts contains "Context Label" and "Stat Value" and "Headline" (sidebar field label assertions)
    - canvas/e2e/archetype-parity.spec.ts contains "archetypeId" (schema fixture uses archetypeId)
    - canvas/e2e/archetype-parity.spec.ts contains "brush" (brush control test)
    - `npx playwright test e2e/archetype-parity.spec.ts` exits 0 with all tests passing
  </acceptance_criteria>
  <done>E2E tests verify sidebar fields render for archetype schemas, text editing works, brush controls appear/disappear based on schema.brush value, and export functions correctly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of archetype-driven pipeline end-to-end</name>
  <files>none</files>
  <action>
User verifies the complete archetype-driven pipeline by generating a real creation and checking editor parity.
  </action>
  <verify>User confirms pipeline produces working archetype-based creations with functional sidebar editing</verify>
  <done>User types "approved" confirming archetype pipeline works end-to-end</done>
  <what-built>
Complete archetype-driven pipeline integration:
1. Pipeline scans archetypes/ directory and injects dynamic list into copy prompt
2. Layout agent receives archetype HTML skeleton for slot-filling instead of freestyling
3. Styling agent declares decorations via DECORATIONS comment
4. SlotSchema is merged and persisted to iteration DB record post-styling
5. Old ARCHETYPE_TEMPLATE_FILES and DEFAULT_ARCHETYPE removed
6. E2E tests verify editor sidebar parity for archetype-based iterations
  </what-built>
  <how-to-verify>
1. Start the dev server: `cd canvas && npm run dev`
2. Open http://localhost:5174 in a browser
3. Open the AI chat sidebar (click the chat icon in the left nav)
4. Type a prompt like "Create an Instagram post about 6X faster deployment times"
5. Watch the pipeline stages progress in the chat sidebar
6. After generation completes, navigate to the created iteration
7. Verify the editor sidebar on the right shows schema-driven fields (text inputs for headline, body copy, etc.)
8. Try editing a field via the sidebar and confirm the iframe preview updates
9. Check the chat sidebar narration mentions the archetype name
10. Type "approved" if the pipeline produces working archetype-based creations with functional sidebar editing
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues for gap closure</resume-signal>
</task>

</tasks>

<verification>
- `cd canvas && npx playwright test e2e/archetype-parity.spec.ts` -- all E2E tests green
- `cd canvas && npx vitest run` -- full unit test suite still green
- Manual: sidebar fields render and are editable for AI-generated archetype creations
</verification>

<success_criteria>
- E2E tests prove sidebar field rendering, text editing, and brush control for archetype-based iterations
- Manual verification confirms pipeline produces working creations end-to-end
- No regressions in existing template-based creation workflow
</success_criteria>

<output>
After completion, create `.planning/phases/20-pipeline-integration-archetype-selection-and-slotschema-attachment/20-03-SUMMARY.md`
</output>
