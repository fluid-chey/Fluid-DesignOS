---
phase: 15-brand-data-architecture
plan: 04
type: execute
wave: 2
depends_on:
  - 15-01
  - 15-02
  - 15-03
files_modified:
  - canvas/src/components/PatternsScreen.tsx
  - canvas/src/components/AssetsScreen.tsx
  - canvas/src/components/TemplatesScreen.tsx
  - canvas/src/components/VoiceGuide.tsx
autonomous: false
requirements:
  - P15-SUBTITLES
must_haves:
  truths:
    - "All four brand pages (Voice Guide, Patterns, Assets, Templates) have functional subtitles under the page title"
    - "Section groupings within pages have brief descriptions"
    - "Empty states show helpful guidance text"
  artifacts:
    - path: "canvas/src/components/VoiceGuide.tsx"
      provides: "Page subtitle"
      contains: "Brand voice rules, messaging frameworks"
    - path: "canvas/src/components/PatternsScreen.tsx"
      provides: "Page subtitle and empty state"
      contains: "Visual building blocks"
    - path: "canvas/src/components/AssetsScreen.tsx"
      provides: "Page subtitle and empty states per category"
      contains: "Fonts, images, logos, and decorative elements"
    - path: "canvas/src/components/TemplatesScreen.tsx"
      provides: "Page subtitle and empty state"
      contains: "Designed examples that show how brand patterns"
  key_links:
    - from: "all four brand page components"
      to: "UI-SPEC copywriting contract"
      via: "verbatim subtitle and empty state copy"
      pattern: "subtitle text matches UI-SPEC"
---

<objective>
Add functional subtitles to all four brand pages and empty state guidance to each page's sections. This is the polish pass that ties together the work from Plans 01-03 into a consistent user experience.

Purpose: Users understand what each brand page is for and get helpful guidance when sections are empty.
Output: Updated VoiceGuide.tsx, PatternsScreen.tsx, AssetsScreen.tsx, TemplatesScreen.tsx with subtitles and empty states.
</objective>

<execution_context>
@/Users/cheyrasmussen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cheyrasmussen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-brand-data-architecture/15-CONTEXT.md
@.planning/phases/15-brand-data-architecture/15-UI-SPEC.md
@.planning/phases/15-brand-data-architecture/15-01-SUMMARY.md
@.planning/phases/15-brand-data-architecture/15-02-SUMMARY.md
@.planning/phases/15-brand-data-architecture/15-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add subtitles and empty states to all four brand pages</name>
  <files>canvas/src/components/VoiceGuide.tsx, canvas/src/components/PatternsScreen.tsx, canvas/src/components/AssetsScreen.tsx, canvas/src/components/TemplatesScreen.tsx</files>
  <read_first>
    - canvas/src/components/VoiceGuide.tsx (full file — find the page title/heading element to add subtitle below)
    - canvas/src/components/PatternsScreen.tsx (full file — find heading and section content areas for subtitle and empty states)
    - canvas/src/components/AssetsScreen.tsx (full file — find heading and each category section for subtitle and empty states)
    - canvas/src/components/TemplatesScreen.tsx (full file — find heading area for subtitle and empty state)
    - .planning/phases/15-brand-data-architecture/15-UI-SPEC.md (lines 170-207 — exact subtitle copy, empty state copy, component spec)
  </read_first>
  <action>
    **Subtitle placement pattern (same for all four pages):**
    Directly below the page heading (h1 or equivalent), add a subtitle paragraph:
    ```tsx
    <p style={{ fontSize: 14, fontWeight: 400, color: '#888', marginTop: 4, marginBottom: 0 }}>
      {subtitleText}
    </p>
    ```

    If a page does not have an explicit h1 heading, add one:
    ```tsx
    <h1 style={{ fontSize: 20, fontWeight: 600, color: '#fff', margin: 0 }}>
      {pageName}
    </h1>
    ```

    **Exact subtitle copy (from UI-SPEC, locked):**

    1. **VoiceGuide.tsx** — Find the page title area. Add subtitle:
       `"Brand voice rules, messaging frameworks, and example copy your team and agents reference"`

    2. **PatternsScreen.tsx** — Add heading "Patterns" and subtitle:
       `"Visual building blocks — color foundations, typographic rules, spacing systems, and compositional techniques"`

    3. **AssetsScreen.tsx** — Add heading "Assets" and subtitle:
       `"Fonts, images, logos, and decorative elements available to the generation pipeline"`

    4. **TemplatesScreen.tsx** — Add heading "Templates" and subtitle:
       `"Designed examples that show how brand patterns come together for specific content types"`

    **Empty state component (inline, reused per page):**
    When a section has zero items, render instead of the grid/list:
    ```tsx
    <div style={{
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'center',
      justifyContent: 'center',
      padding: '48px 24px',
      textAlign: 'center',
    }}>
      {/* Category-appropriate 40x40 inline SVG icon in var(--text-muted) #555 */}
      <h4 style={{ fontSize: 14, fontWeight: 600, color: '#e0e0e0', margin: '12px 0 4px' }}>
        {emptyHeading}
      </h4>
      <p style={{ fontSize: 12, fontWeight: 400, color: '#888', margin: 0, maxWidth: 300 }}>
        {emptyBody}
      </p>
    </div>
    ```

    **Exact empty state copy (from UI-SPEC copywriting contract):**

    PatternsScreen — when a collapsible section has no patterns:
    - Heading: "No patterns in this section"
    - Body: "Add brand context from the DB seeder or create a new entry."

    AssetsScreen — per category when filtered result is empty:
    - Fonts: Heading: "No fonts yet" / Body: "No fonts yet. Sync from DAM or add font files manually."
    - Images: Heading: "No images yet" / Body: "Sync from DAM to import photography and illustrations."
    - Brand Elements: Heading: "No brand elements yet" / Body: "Sync from DAM to import logos and wordmarks."
    - Decorations: Heading: "No decorations yet" / Body: "Sync from DAM to import hand-drawn circles, brushstrokes, and textures."

    TemplatesScreen — when no template cards exist:
    - Heading: "No templates yet"
    - Body: "Templates are seeded automatically on first app startup."

    **Empty state icons (simple inline SVGs, 40x40, stroke only, color #555):**
    - Patterns: sparkle/diamond shape
    - Fonts: "A" letter icon
    - Images: mountain/landscape icon
    - Brand Elements: shield/badge icon
    - Decorations: paintbrush icon
    - Templates: layout/grid icon

    Keep icons minimal — 3-4 path elements max. These are placeholders, not polished illustrations.

    **Error state text (from UI-SPEC):**
    If any page already has error handling, ensure error messages match:
    - Fetch failure: "Failed to load [page name] data. Check the server is running and refresh."
    - Save failure: "Save failed. Your change was not applied — the previous value has been restored."
  </action>
  <verify>
    <automated>cd /Users/cheyrasmussen/Fluid-DesignOS/canvas && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - VoiceGuide.tsx contains "Brand voice rules, messaging frameworks, and example copy your team and agents reference"
    - PatternsScreen.tsx contains "Visual building blocks"
    - AssetsScreen.tsx contains "Fonts, images, logos, and decorative elements available to the generation pipeline"
    - TemplatesScreen.tsx contains "Designed examples that show how brand patterns come together for specific content types"
    - PatternsScreen.tsx contains "No patterns in this section"
    - AssetsScreen.tsx contains "No fonts yet" AND "No images yet" AND "No brand elements yet" AND "No decorations yet"
    - TemplatesScreen.tsx contains "No templates yet"
    - All subtitles use fontSize 14, fontWeight 400, color '#888'
    - All existing vitest tests pass
  </acceptance_criteria>
  <done>All four brand pages have functional subtitles and empty state guidance matching UI-SPEC copy exactly</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all four brand pages</name>
  <files>canvas/src/components/VoiceGuide.tsx, canvas/src/components/PatternsScreen.tsx, canvas/src/components/AssetsScreen.tsx, canvas/src/components/TemplatesScreen.tsx</files>
  <action>
    Human visually verifies the complete Phase 15 brand data reorganization across all four brand pages. Start dev server if not running: `cd canvas && npm run dev`. Open browser and navigate to each page.
  </action>
  <verify>User confirms visual correctness of all four brand pages</verify>
  <done>User types "approved" confirming all pages look correct</done>
  <what-built>
    Complete Phase 15 brand data reorganization:
    1. PatternsScreen — full React component with Foundations/Rules collapsible sections, sandboxed iframe previews, inline editing
    2. AssetsScreen — four category tabs (Fonts/Images/Brand Elements/Decorations), inline description editing, category descriptions
    3. TemplatesScreen — unified page (no tabs), collapsible social media design rules, per-template cards with design rules
    4. All four pages — functional subtitles and empty state guidance
  </what-built>
  <how-to-verify>
    1. Start dev server: `cd canvas && npm run dev`
    2. Open browser to http://localhost:5180 (or configured port)
    3. Navigate to each brand page via left nav:

    **Patterns page:**
    - Verify subtitle text visible below heading
    - Verify two collapsible sections: Foundations (3 patterns) and Rules (10 patterns)
    - Expand/collapse sections — smooth animation
    - Each pattern card shows HTML preview (visual, not code)
    - Click pattern text — edit mode activates, blur saves

    **Assets page:**
    - Verify subtitle text visible
    - Verify four category tabs: Fonts, Images, Brand Elements, Decorations with counts
    - Click each tab — assets filter correctly
    - Category description text appears below active tab
    - Click "Add description..." on an asset — edit mode, save, verify persistence

    **Templates page:**
    - Verify subtitle text visible
    - Verify NO tab bar
    - "Social Media Design Rules" collapsible section at top (collapsed by default)
    - Template cards with descriptive names (no "archetype" text visible anywhere)
    - Per-template "Design Rules" collapsible on each card

    **Voice Guide page:**
    - Verify subtitle text visible below heading
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
- All four brand pages render with subtitles
- Empty state components display when relevant sections are empty
- Error states use correct copy
- vitest suite passes
</verification>

<success_criteria>
- Every brand page has a functional subtitle
- Empty states show guidance text per UI-SPEC copywriting contract
- Visual consistency across all four pages
- User approves visual verification
</success_criteria>

<output>
After completion, create `.planning/phases/15-brand-data-architecture/15-04-SUMMARY.md`
</output>
