---
phase: 18-archetype-system-research-and-component-design
plan: 03
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - archetypes/stat-hero-single/index.html
  - archetypes/stat-hero-single/schema.json
  - archetypes/stat-hero-single/README.md
autonomous: false
requirements:
  - ARCH-04

must_haves:
  truths:
    - "stat-hero-single archetype has index.html renderable at 1080x1080 with placeholder content"
    - "stat-hero-single schema.json matches the SlotSchema TypeScript interface exactly"
    - "schema.json has archetypeId field (not templateId) and brush: null"
    - "Every CSS selector in schema.json fields has a matching element in index.html"
    - "schema.json parses as valid JSON"
    - "User has visually verified the archetype renders correctly"
  artifacts:
    - path: "archetypes/stat-hero-single/index.html"
      provides: "Brandless stat-hero structural skeleton with placeholder text"
      contains: "stat-number"
      min_lines: 40
    - path: "archetypes/stat-hero-single/schema.json"
      provides: "SlotSchema definition for stat-hero-single"
      contains: "archetypeId"
    - path: "archetypes/stat-hero-single/README.md"
      provides: "Archetype documentation — purpose, pattern, content type"
      contains: "stat-hero-single"
  key_links:
    - from: "archetypes/stat-hero-single/schema.json"
      to: "archetypes/stat-hero-single/index.html"
      via: "CSS selectors in fields[].sel must match HTML class names"
      pattern: "\\.stat-number|\\.headline|\\.body-copy"
    - from: "archetypes/stat-hero-single/schema.json"
      to: "canvas/src/lib/slot-schema.ts"
      via: "JSON structure matches SlotSchema interface"
      pattern: "archetypeId.*width.*height.*fields"
---

<objective>
Build the proof-of-concept archetype (stat-hero-single) end-to-end and validate it with the user.

Purpose: Validates the entire archetype format spec before Phase 19 commits to building 6-8 archetypes. If the PoC works correctly (renders, schema is compatible, placeholder content is legible), the spec is confirmed. If issues arise, they're caught before mass implementation.
Output: 3 files (index.html, schema.json, README.md) + user visual verification
</objective>

<execution_context>
@/Users/cheyrasmussen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cheyrasmussen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-archetype-system-research-and-component-design/18-CONTEXT.md
@.planning/phases/18-archetype-system-research-and-component-design/18-RESEARCH.md
@.planning/phases/18-archetype-system-research-and-component-design/18-01-SUMMARY.md

<interfaces>
<!-- Full SlotSchema interface — schema.json MUST match this exactly -->
From canvas/src/lib/slot-schema.ts:
```typescript
export interface SlotSchema {
  templateId?: string;          // NOT used for archetypes
  width: number;
  height: number;
  fields: SlotField[];
  brush?: string | null;        // MUST be null for archetypes
  brushLabel?: string;
  brushAdditional?: ReadonlyArray<{ sel: string; label: string }>;
  carouselCount?: number;
}

export type SlotField = TextField | ImageField | DividerField;

export interface TextField {
  type: 'text';
  sel: string;
  label: string;
  mode: FieldMode;  // 'text' | 'pre' | 'br'
  rows?: number;
}
```

<!-- Reference: existing stat-proof template (the branded version of this pattern) -->
From templates/social/stat-proof.html (CSS structure to reference):
- .stat-number: giant number, absolute positioned, ~260px font
- .context-label: small uppercase label above stat
- .headline: secondary statement, ~82px font
- .body-copy: supporting text, ~24px font
- .category span: side label
- All content uses absolute positioning with px values

From canvas/src/server/api-pipeline.ts:
```typescript
const ARCHETYPE_TEMPLATE_FILES: Record<string, string> = {
  'stat-proof': path.join(PROJECT_ROOT, 'templates/social/stat-proof.html'),
  // Phase 20 will add: 'stat-hero-single': path.join(PROJECT_ROOT, 'archetypes/stat-hero-single/index.html')
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build stat-hero-single archetype (index.html + schema.json + README.md)</name>
  <files>archetypes/stat-hero-single/index.html, archetypes/stat-hero-single/schema.json, archetypes/stat-hero-single/README.md</files>
  <read_first>
    - archetypes/SPEC.md (CRITICAL — follow every convention: HTML rules, schema rules, selector convention, anti-patterns)
    - canvas/src/lib/slot-schema.ts (full file — verify schema.json matches SlotSchema interface, understand collectTransformTargets behavior)
    - canvas/src/lib/template-configs.ts (reference: t1Quote for field pattern examples, particularly sel/label/mode/rows conventions)
    - templates/social/stat-proof.html (the branded version of this exact pattern — use as structural reference for positioning and proportions, but strip all brand elements)
  </read_first>
  <action>
Create the `archetypes/stat-hero-single/` directory with 3 files:

**1. index.html** — Brandless structural skeleton for a stat-hero layout at 1080x1080. Structure derived from templates/social/stat-proof.html but with all brand elements stripped:

```html
<!DOCTYPE html>
<!-- archetype: stat-hero-single | target: 1080x1080 -->
<html lang="en">
<head>
<meta charset="UTF-8"/>
<style>
  * { box-sizing: border-box; margin: 0; padding: 0; }
  body {
    width: 1080px;
    height: 1080px;
    overflow: hidden;
    background: #111;
    position: relative;
    font-family: sans-serif;
  }

  .decorative-zone {
    position: absolute;
    top: 0; right: 0; bottom: 0; left: 0;
    pointer-events: none;
    z-index: 0;
  }

  .category {
    position: absolute;
    left: 68px;
    top: 68px;
    display: flex;
    align-items: center;
    gap: 8px;
  }
  .category span {
    font-size: 12px;
    font-weight: 600;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: rgba(255,255,255,0.5);
  }

  .context-label {
    position: absolute;
    left: 68px;
    top: 160px;
    font-size: 14px;
    font-weight: 500;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: rgba(255,255,255,0.5);
  }

  .stat-number {
    position: absolute;
    left: 68px;
    top: 190px;
    font-weight: 900;
    font-size: 260px;
    line-height: 0.85;
    color: #ffffff;
    letter-spacing: -0.04em;
  }

  .headline {
    position: absolute;
    left: 68px;
    top: 520px;
    width: 900px;
    font-weight: 900;
    font-size: 72px;
    line-height: 0.95;
    color: #ffffff;
    text-transform: uppercase;
    letter-spacing: -0.03em;
  }

  .body-copy {
    position: absolute;
    left: 68px;
    top: 740px;
    width: 750px;
    font-size: 24px;
    line-height: 1.45;
    color: rgba(255,255,255,0.45);
  }
</style>
</head>
<body>
  <div class="decorative-zone"></div>

  <!-- SLOT: category -->
  <div class="category">
    <span>PAYMENT PROCESSING</span>
  </div>

  <!-- SLOT: context-label -->
  <div class="context-label">APPROVAL RATE ACROSS ALL PSPs</div>

  <!-- SLOT: stat-number -->
  <div class="stat-number">94%</div>

  <!-- SLOT: headline -->
  <div class="headline">WHILE COMPETITORS LEAVE MONEY ON THE TABLE</div>

  <!-- SLOT: body-copy -->
  <p class="body-copy">Seven PSPs. Automatic retry logic. Every transaction gets its best shot at clearing. The difference between 87% and 94% is millions in recovered revenue.</p>
</body>
</html>
```

Adjust the exact pixel positions as needed to create a visually balanced layout. The key constraint: all positioning is absolute with explicit px values. The proportions should match the stat-proof.html reference (giant stat takes ~40% of canvas height, headline below, body copy at bottom).

**2. schema.json** — SlotSchema matching the slot-schema.ts interface exactly:

```json
{
  "archetypeId": "stat-hero-single",
  "width": 1080,
  "height": 1080,
  "fields": [
    { "type": "text", "sel": ".category span", "label": "Side Label",    "mode": "text", "rows": 1 },
    { "type": "text", "sel": ".context-label",  "label": "Context Label", "mode": "text", "rows": 1 },
    { "type": "text", "sel": ".stat-number",    "label": "Stat Value",    "mode": "text", "rows": 1 },
    { "type": "text", "sel": ".headline",       "label": "Headline",      "mode": "text", "rows": 2 },
    { "type": "text", "sel": ".body-copy",      "label": "Body Copy",     "mode": "pre",  "rows": 4 }
  ],
  "brush": null
}
```

Verify every field's `sel` value matches a CSS class in index.html. The `.category span` selector matches the `<span>` inside the `.category` div (consistent with t1-quote template convention).

**3. README.md** — Archetype documentation:

```markdown
# stat-hero-single

**Archetype ID:** stat-hero-single
**Platform:** Instagram Square (1080 x 1080)
**Structural pattern:** One giant stat (number + unit) occupies 40% of canvas height; headline below; body copy at bottom.

## When to use

- Post leads with a single compelling metric (acceptance rate, revenue growth, conversion lift)
- The number itself is the hook — it should be visually dominant
- Supporting text explains why the number matters

## When NOT to use

- Post leads with a problem or emotion before revealing data
- Multiple stats of equal importance (use data-dashboard instead)
- The stat needs a chart or visual comparison to be meaningful

## Content type

Data-driven proof point, performance metric, achievement milestone

## Components used

- stat-card (stat-number, context-label)
- eyebrow-headline (category, headline)
- body-text (body-copy)

## Slots

| Slot | Type | Selector | Description |
|------|------|----------|-------------|
| Side Label | text | .category span | Category/topic label (e.g., "PAYMENT PROCESSING") |
| Context Label | text | .context-label | Descriptor above stat (e.g., "APPROVAL RATE") |
| Stat Value | text | .stat-number | The giant metric (e.g., "94%") |
| Headline | text | .headline | Supporting statement about the stat's meaning |
| Body Copy | text | .body-copy | 1-2 sentences of supporting explanation |

## Reference

Derived from `templates/social/stat-proof.html` (branded version with Fluid assets).
```
  </action>
  <verify>
    <automated>node -e "const s = JSON.parse(require('fs').readFileSync('archetypes/stat-hero-single/schema.json','utf-8')); if(s.archetypeId !== 'stat-hero-single') throw 'bad archetypeId'; if(s.width !== 1080 || s.height !== 1080) throw 'bad dims'; if(s.fields.length < 4) throw 'too few fields'; if(s.brush !== null) throw 'brush must be null'; if(s.templateId) throw 'must not have templateId'; console.log('PASS: schema.json is valid')"</automated>
  </verify>
  <acceptance_criteria>
    - archetypes/stat-hero-single/index.html contains "archetype: stat-hero-single"
    - archetypes/stat-hero-single/index.html contains "width: 1080px" and "height: 1080px"
    - archetypes/stat-hero-single/index.html contains ".stat-number", ".headline", ".body-copy", ".context-label", ".category span"
    - archetypes/stat-hero-single/index.html contains "font-family: sans-serif" (NOT a brand font)
    - archetypes/stat-hero-single/index.html contains "decorative-zone"
    - archetypes/stat-hero-single/index.html contains at least 5 `<!-- SLOT:` comments
    - archetypes/stat-hero-single/index.html does NOT contain inline style= attributes
    - archetypes/stat-hero-single/index.html does NOT contain any /api/brand-assets/ or /fluid-assets/ URLs
    - archetypes/stat-hero-single/schema.json parses as valid JSON
    - archetypes/stat-hero-single/schema.json contains "archetypeId": "stat-hero-single"
    - archetypes/stat-hero-single/schema.json contains "brush": null
    - archetypes/stat-hero-single/schema.json does NOT contain "templateId"
    - archetypes/stat-hero-single/schema.json has 5 fields
    - archetypes/stat-hero-single/README.md contains "When to use" and "When NOT to use"
  </acceptance_criteria>
  <done>stat-hero-single archetype exists with index.html (renderable at 1080x1080 with placeholder text), schema.json (valid SlotSchema with archetypeId, 5 fields, brush: null), and README.md (usage guidance).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify PoC archetype renders correctly</name>
  <files>archetypes/stat-hero-single/index.html</files>
  <action>User visually verifies the proof-of-concept archetype by opening index.html in a browser and confirming the layout matches the structural pattern described in SPEC.md.</action>
  <what-built>The stat-hero-single proof-of-concept archetype — a brandless structural skeleton for a stat-hero social post layout (1080x1080). This validates the archetype format spec before Phase 19 builds the full library.</what-built>
  <how-to-verify>
    1. Open `archetypes/stat-hero-single/index.html` in a browser
    2. Verify: page renders at 1080x1080 with dark (#111) background
    3. Verify: giant "94%" stat number dominates upper half (~40% of canvas)
    4. Verify: "PAYMENT PROCESSING" side label in small uppercase at top
    5. Verify: "APPROVAL RATE ACROSS ALL PSPs" context label above stat
    6. Verify: "WHILE COMPETITORS LEAVE MONEY ON THE TABLE" headline below stat
    7. Verify: Body copy paragraph at bottom in lighter opacity
    8. Verify: all text is white/gray on dark background, sans-serif font
    9. Verify: NO brand assets visible (no brushstrokes, no logos, no brand fonts)
    10. Verify: layout looks like a clean wireframe of the stat-proof template
    11. Open `archetypes/stat-hero-single/schema.json` and confirm it has 5 fields matching the visible slots
  </how-to-verify>
  <verify>User confirms visual rendering matches spec expectations</verify>
  <done>User has approved the stat-hero-single PoC archetype, validating the format spec for Phase 19.</done>
  <resume-signal>Type "approved" if the archetype looks correct and validates the format spec, or describe any layout/content issues to fix.</resume-signal>
</task>

</tasks>

<verification>
- `node -e "JSON.parse(require('fs').readFileSync('archetypes/stat-hero-single/schema.json','utf-8'))"` succeeds (valid JSON)
- `grep -c "SLOT:" archetypes/stat-hero-single/index.html` returns 5
- `grep "archetypeId" archetypes/stat-hero-single/schema.json` shows "stat-hero-single"
- `grep "templateId" archetypes/stat-hero-single/schema.json` returns nothing
- User has approved the visual rendering
</verification>

<success_criteria>
The stat-hero-single PoC archetype renders correctly at 1080x1080, has a valid schema.json matching the SlotSchema interface, and the user has visually confirmed it validates the format spec. This proves the archetype system design works before Phase 19 scales to 6-8 archetypes.
</success_criteria>

<output>
After completion, create `.planning/phases/18-archetype-system-research-and-component-design/18-03-SUMMARY.md`
</output>
