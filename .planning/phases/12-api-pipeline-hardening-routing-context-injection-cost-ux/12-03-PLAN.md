---
phase: 12-api-pipeline-hardening-routing-context-injection-cost-ux
plan: "03"
type: execute
wave: 2
depends_on: ["12-01", "12-02"]
files_modified:
  - canvas/src/server/api-pipeline.ts
  - canvas/src/__tests__/api-pipeline.test.ts
  - canvas/src/__tests__/VoiceGuide.test.tsx
  - canvas/src/__tests__/skill-paths.test.ts
  - canvas/src/hooks/useGenerationStream.ts
  - canvas/src/server/watcher.ts
  - CLAUDE.md
autonomous: true
requirements: [CLEAN-06, CLEAN-07]

must_haves:
  truths:
    - "Pre-existing test failures are fixed or intentionally removed with documented reasoning"
    - "No stale references to CLI generation, old phase numbers, or removed modules remain in active code"
    - "MCP tools are audited and documented as active or flagged for future removal"
    - "The full test suite has fewer failures than the pre-phase baseline of ~9-14"
    - "CLAUDE.md validation tools section reflects the DB-backed reality"
  artifacts:
    - path: "canvas/src/__tests__/VoiceGuide.test.tsx"
      provides: "Fixed VoiceGuide tests (pre-existing failures resolved)"
    - path: "canvas/src/__tests__/api-pipeline.test.ts"
      provides: "Cleaned pipeline tests with no stale CLI references"
    - path: "CLAUDE.md"
      provides: "Updated project instructions reflecting DB-backed tools"
  key_links:
    - from: "canvas/src/__tests__/"
      to: "canvas/src/"
      via: "test imports"
      pattern: "import.*from"
---

<objective>
Fix pre-existing test failures, audit MCP tools for relevance, clean up stale references across the codebase, and update project documentation to reflect the post-cleanup reality. This is the coherence verification pass that ensures everything tells a consistent story.

Purpose: Close the phase with a green(er) test suite, audited infrastructure, and accurate documentation.
Output: Reduced test failures, MCP audit notes, updated CLAUDE.md.
</objective>

<execution_context>
@/Users/cheyrasmussen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cheyrasmussen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-api-pipeline-hardening-routing-context-injection-cost-ux/12-CONTEXT.md
@.planning/phases/12-api-pipeline-hardening-routing-context-injection-cost-ux/12-01-SUMMARY.md
@.planning/phases/12-api-pipeline-hardening-routing-context-injection-cost-ux/12-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix pre-existing test failures and audit stale test code</name>
  <files>canvas/src/__tests__/VoiceGuide.test.tsx, canvas/src/__tests__/api-pipeline.test.ts, canvas/src/__tests__/skill-paths.test.ts, canvas/src/__tests__/prompt-sidebar.test.tsx</files>
  <read_first>
    - canvas/src/__tests__/VoiceGuide.test.tsx (full file — understand the tab active state assertion failure from Phase 11-02)
    - canvas/src/components/VoiceGuide.tsx (full file — understand current implementation to fix test)
    - canvas/src/__tests__/api-pipeline.test.ts (full file — check for any remaining stale references after Plan 01 cleanup)
    - canvas/src/__tests__/skill-paths.test.ts (full file — understand canvas-active sentinel check failure)
    - canvas/src/__tests__/prompt-sidebar.test.tsx (full file — understand gallery/prompt sidebar rendering failures)
  </read_first>
  <action>
Run the full test suite first to establish the current failure baseline:
`cd canvas && npx vitest run --reporter=verbose 2>&1 | grep -E "FAIL|fail" | head -20`

Then fix each pre-existing failure category:

**1. VoiceGuide.test.tsx — tab active state assertion:**
Phase 11-02 changed VoiceGuide from static ?raw imports to DB fetch. The test likely still expects synchronous doc availability or a specific initial active tab. Fix:
- Mock `fetch('/api/voice-guide')` to return the 13 docs
- Update assertions to account for async loading state
- Ensure the active tab assertion matches the new `activeDocSlug` initialization pattern (empty string -> docs[0].slug on load)

**2. skill-paths.test.ts — canvas-active sentinel check:**
Read the test to understand what it checks. If it's testing the `.fluid/canvas-active` sentinel file existence for skill output routing, and that path still exists in the codebase, fix the test environment setup. If the sentinel feature was superseded by API pipeline, mark the test as skipped with a comment explaining why.

**3. prompt-sidebar.test.tsx — gallery/prompt sidebar rendering:**
Phase 11-04 restructured PromptSidebar to Claude-style layout. Tests likely assert old DOM structure. Fix:
- Update selectors to match new layout (bottom input, message scroll area)
- If tests are testing features that were removed (e.g., "Recent Sessions"), remove those tests
- Keep tests for core functionality (prompt submission, message display)

**4. api-pipeline.test.ts — remaining stale references:**
After Plan 01 removed engine routing tests, scan for any other stale patterns:
- Tests referencing `getFallbackPrompt` signature changes from Phase 11-03 (should have been fixed, but verify)
- Any test importing removed watcher.ts functions
- Check mock setup for consistency with current api-pipeline.ts exports

**5. For any test that cannot be reasonably fixed (e.g., AppShell ResizeObserver in jsdom):**
Add `describe.skip('AppShell ...', () => { ... })` with a comment: `// Skipped: ResizeObserver not available in jsdom — needs browser-env test runner`
Do NOT delete test files that test still-existing components — skip with documented reason.

**Goal:** Reduce the failure count from ~9-14 to as close to 0 as practical.
  </action>
  <verify>
    <automated>cd /Users/cheyrasmussen/Fluid-DesignOS/canvas && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd canvas && npx vitest run 2>&1 | grep "Tests.*failed"` shows fewer failures than the pre-phase baseline of 9-14
    - VoiceGuide.test.tsx either passes or is skipped with documented reason
    - skill-paths.test.ts either passes or is skipped with documented reason
    - prompt-sidebar.test.tsx either passes or is skipped with documented reason
    - No test file imports from deleted modules (generate-endpoint.test.ts was deleted in Plan 01)
    - `grep -rn "rules\.json" canvas/src/__tests__/` returns 0 matches
  </acceptance_criteria>
  <done>Pre-existing test failures are resolved (fixed or skipped with documentation). Test suite failure count is reduced. No stale imports or references to removed code.</done>
</task>

<task type="auto">
  <name>Task 2: MCP audit, stale reference sweep, and CLAUDE.md update</name>
  <files>CLAUDE.md, canvas/mcp/server.ts, canvas/src/hooks/useGenerationStream.ts, canvas/src/server/watcher.ts</files>
  <read_first>
    - canvas/mcp/server.ts (full file — understand which 5 MCP tools exist)
    - canvas/mcp/tools/push-asset.ts (first 20 lines — check if it calls HTTP API or direct DB)
    - canvas/src/hooks/useGenerationStream.ts (full file — check for CLI-era references)
    - canvas/src/server/watcher.ts (after Plan 01 cleanup — verify no stale references remain)
    - CLAUDE.md (full file — understand current content)
    - .mcp.json (MCP configuration — check what's configured)
  </read_first>
  <action>
**1. MCP Tool Audit:**

Check each of the 5 MCP tools in `canvas/mcp/server.ts`:
- `push_asset` — Used by pipeline agents to push generated HTML. Likely ACTIVE (called by subagent prompts in skill files).
- `read_annotations` — Used by agents to read iteration feedback. Check if any skill/prompt references it.
- `read_statuses` — Used by agents to check variation statuses.
- `read_history` — Used by agents to read iteration chains.
- `iterate_request` — Used to signal iteration rounds.

For each tool, search the codebase:
```
grep -rn "push_asset\|read_annotations\|read_statuses\|read_history\|iterate_request" canvas/src/ skills/ --include="*.ts" --include="*.md" --include="*.cjs"
```

**Decision criteria:**
- If a tool is referenced in API pipeline prompts or skill files -> KEEP (still active)
- If a tool was only referenced in CLI-era `--allowedTools` args (now removed by Plan 01) -> FLAG for future removal but do NOT remove yet (MCP tools may be used by external Claude Code sessions)

Document findings as a comment block at the top of `canvas/mcp/server.ts`:
```typescript
/**
 * MCP Tool Audit (Phase 12, 2026-03-17):
 * - push_asset: ACTIVE — used by pipeline subagent prompts
 * - read_annotations: [ACTIVE|INACTIVE] — [reason]
 * - read_statuses: [ACTIVE|INACTIVE] — [reason]
 * - read_history: [ACTIVE|INACTIVE] — [reason]
 * - iterate_request: [ACTIVE|INACTIVE] — [reason]
 */
```

**2. Stale reference sweep in useGenerationStream.ts:**

Check for:
- References to `engine: 'cli'` or CLI-specific SSE event handling
- References to `sessionId` from the old iterate mode
- References to `activeChild` or CLI process tracking

Fix any stale references found.

**3. Stale reference sweep in watcher.ts (post-Plan 01):**

Verify Plan 01 cleanup was complete:
- No orphaned comments referencing CLI
- No dead imports
- No unreachable code paths
- No variables declared but never used

**4. Update CLAUDE.md:**

Update the `## CLI Tools` section to reflect the new reality:
```markdown
## CLI Tools

Validation tools live in `tools/`. Run after generating output:
- `node tools/brand-compliance.cjs <file>` -- validate HTML against brand rules (reads from SQLite DB)
- `node tools/schema-validation.cjs <file>` -- validate .liquid files against Gold Standard schema
- `node tools/dimension-check.cjs <file> --target <type>` -- check dimensions
- `node tools/scaffold.cjs <section-name>` -- generate Gold Standard .liquid skeleton

Note: Validation tools read brand data from the SQLite database (canvas/.fluid/fluid.db).
The app must be run at least once to seed the database before validation tools will work.
```

Remove any reference to `compile-rules.cjs` if present.
  </action>
  <verify>
    <automated>cd /Users/cheyrasmussen/Fluid-DesignOS && grep -c "compile-rules\|rules\.json" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "compile-rules" CLAUDE.md` returns 0 matches
    - `grep "rules.json" CLAUDE.md` returns 0 matches
    - `grep "MCP Tool Audit" canvas/mcp/server.ts` returns 1 match (audit comment added)
    - `grep -c "engine.*cli\|activeChild\|ChildProcess" canvas/src/hooks/useGenerationStream.ts` returns 0
    - CLAUDE.md CLI Tools section mentions "SQLite DB" or "database"
    - `grep "reads from SQLite\|reads from.*DB\|SQLite database" CLAUDE.md` finds at least 1 match
  </acceptance_criteria>
  <done>MCP tools audited with status documented. Stale references cleaned from useGenerationStream.ts and any remaining spots. CLAUDE.md updated to reflect DB-backed validation tools. Project documentation is coherent with codebase reality.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `cd canvas && npx vitest run` shows reduced failure count (target: <5 failures, ideally 0)
2. `grep -rn "rules\.json\|compile-rules" tools/ canvas/src/ CLAUDE.md` returns 0 matches (no stale references)
3. `grep -rn "engine.*=.*cli\|spawn.*claude" canvas/src/` returns 0 matches (no CLI remnants)
4. MCP audit documented in canvas/mcp/server.ts
5. CLAUDE.md accurately describes current tool behavior
</verification>

<success_criteria>
- Test suite is cleaner (fewer failures than pre-phase baseline)
- MCP tools audited and documented
- No stale CLI or rules.json references in active code
- CLAUDE.md reflects the DB-backed, API-only generation reality
- Infrastructure tells a consistent story
</success_criteria>

<output>
After completion, create `.planning/phases/12-api-pipeline-hardening-routing-context-injection-cost-ux/12-03-SUMMARY.md`
</output>
